{
  "name": "punch-client-site",
  "version": "2.0.0",
  "private": true,
  "description": "Punch Minecraft Client — Polar SPA + Express API",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "dev:web": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "build:web": "npm run build --prefix web",
    "start": "node server.js",
    "loader:ui": "node scripts/sync-loader-ui.js",
    "lint": "next lint"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^11.0.5"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
