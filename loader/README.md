# Punch Loader (C++ + WebView2)

Окно **920×540**, фиксированный размер. При старте — консоль VirtuGuard, затем GUI с Yacaptcha.

## Сборка

```bat
cd loader
build.bat
```

Сайт: `npm start` на `:3001`.  
UI sync: `npm run loader:ui`
